#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Print alphabets
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char  alpha;

	for (alpha = 'a'; alpha <= 'z'; alpha++)
		putchar (alpha);
		putchar ('\n');

	retun (0);
}
