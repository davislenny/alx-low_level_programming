#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Print alphabets
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	for (alpha = 'a'; alpha <= 'z'; alpha++)
	{
		putchar (alpha);
		putchar ('\n');
	}
	retun (0);
}
