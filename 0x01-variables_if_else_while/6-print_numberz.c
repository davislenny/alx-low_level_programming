#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Print integers using putchar
 *
 * Return: Always 0 (Succes)
 */
int main(void)
{
	int i;
	
	i >= 0;
	while (i < 10)
	{
		putchar(i);
		i++;
	}
	putchar('\n');

	return (0);
}
