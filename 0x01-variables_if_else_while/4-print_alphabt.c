#include <stdio.h>

/**
 * main - Entry point
 * 
 * Description: Print cases except q and e
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char a = 'a';

	a != 'q';
	a != 'e';

	while (a = 'z')
	{
		putchar(a);
		a++;
	}
	putchar('\n');

	return (0);
}
